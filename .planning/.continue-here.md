---
context: default
phase: production-ci-closeout
task: 8
total_tasks: 8
status: complete
last_updated: 2026-07-26T01:50:14+08:00
---

# 正式站 CI 與 GitHub 收工交接

## 必守限制

- [x] production 作品、相簿順序、帳號、權限與設定在 PostgreSQL `goodjob_site`，圖片本體在 R2/CDN；部署禁止包含 `data/`。
- [x] production 每次只傳本次明確變更的檔案，禁止 `scp -r *`、整站 `rsync --delete` 或在站台目錄直接 `git pull`。
- [x] 遠端操作固定使用 Windows OpenSSH `/mnt/c/Windows/System32/OpenSSH/ssh.exe`；本 session 的 WSL `ssh` 沒有可用主機金鑰。
- [x] 部署前後分別核對 `articles`、`article_images`、`accounts`，不可把圖片清單誤認成 `articles.images` 欄位。

## 完成狀態

2026-07-26 正式站 CI／首頁／服務頁已全部完成並上線：

- 首頁採左文字、右側 `3:2` 橫式 Hero，移除中段大面積空排。
- GOODJOB DESIGN 官方四色為 SALT `#F2F0EB`、INK `#222322`、STONE `#817C74`、POINT `#9B3E35`。
- 網站加入低對比淡方格底與紅色方形定位點。
- 相簿 hover 預覽、播放後作品詳情與 Lightbox 已套用相同 CI。
- 四類服務頁的代表案例各補齊為 4 個，全部引用既有作品，不改資料庫。
- Topbar 使用 `assets/images/logo-topbar-ci.png` 完整品牌字標。
- 首頁品牌敘述固定為兩行。

Production 驗證結果：

- `https://goodjob.weddingwishlove.com/`：HTTP 200
- `murayama-goodjob.service`：active
- PostgreSQL：`articles=64`、`article_images=632`、`accounts=3`
- 本次沒有改動任何正式作品、相簿、帳號或權限紀錄

## GitHub 整理

- `master` 已在上一輪推送至 `origin/master`，推送前後分歧為 `0 / 0`。
- 原本未追蹤的 `AGENTS.md` 已納入專案規範。
- `README.md` 已改成目前鹽白 CI、PostgreSQL／R2 架構、正確目錄與安全部署方式。
- `DESIGN.md` 已移除過時的 Netflix／JSON 單一管理員架構，改為目前品牌與系統決策。
- 本檔、`.planning/HANDOFF.json` 與根目錄 `handoff.md` 已移除「尚未推送」的過時敘述。
- GitHub repository homepage 與 topics 已補齊。

完成本次收工提交並推送後，本機 `master` 與 `origin/master` 應一致，工作樹應為 clean。

## Commit 序列

| Commit | 內容 | Production |
|---|---|---|
| `17b7ac5` | 首頁官方 CI 色票、方點與方格背景 | 已部署 |
| `85697a4` | 相簿預覽與作品詳情 CI | 已部署 |
| `be058e3` | 四類服務頁各 4 個代表案例 | 已部署 |
| `8fb6aab` | Topbar 完整品牌字標 | 已部署 |
| `af0aa41` | 首頁品牌敘述固定兩行 | 已部署 |
| `4459dfd` | 正式站 CI 收工交接 | 已推送 |
| 本次收工提交 | GitHub README、設計文件、規範與交接整理 | 文件，不需部署 |

## 尚未完成

無。Staging parity 不屬於本次正式站收工範圍；未來只有使用者另行要求時才處理。

## 下一次開始方式

1. 讀取本檔、`AGENTS.md` 與 `.planning/HANDOFF.json`。
2. 執行 `git fetch origin && git status --short --branch`，應看到 clean 且無 ahead／behind。
3. 新網站需求開始前，先唯讀核對 production service 與 PostgreSQL `64 / 632 / 3` 基準。
4. 只修改與部署新需求明確涉及的檔案。

## 重要文件

1. `AGENTS.md` — 專案規範與資料邊界。
2. `README.md` — GitHub 首頁與開發／部署入口。
3. `DESIGN.md` — CI 與架構決策。
4. `docs/村山良作-部署資訊清單.md` — 正式主機、service 與驗證方式。
5. `docs/村山良作-部署護欄與復原-20260506.md` — 禁止部署方式與復原程序。
6. `docs/村山良作-R2-CDN-維運手冊-20260417.md` — 圖片資產維運。
7. `docs/村山良作-新增作品SOP.md` — 新作品上架流程。
